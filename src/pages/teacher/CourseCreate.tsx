/**
 * Course creation page for teachers
 */

import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import * as z from "zod";
import { useTranslation } from "react-i18next";
import { format } from "date-fns";
import { CalendarIcon } from "lucide-react";

import TeacherLayout from "@/components/layout/TeacherLayout";
import { useAuthContext } from "@/contexts/AuthContext";
import { useCourseMutations } from "@/hooks/useClasses";
import { ROUTES } from "@/config/constants";

import { Button } from "@/components/ui/button";
import {
  Breadcrumb,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbList,
  BreadcrumbPage,
  BreadcrumbSeparator,
} from "@/components/ui/breadcrumb";
import {
  Form,
  FormControl,
  FormDescription,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
} from "@/components/ui/form";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { Calendar } from "@/components/ui/calendar";
import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from "@/components/ui/popover";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";

// Form validation schema
const courseFormSchema = z.object({
  name: z
    .string()
    .min(3, "Navn må være minst 3 tegn")
    .max(100, "Navn kan ikke være mer enn 100 tegn"),
  description: z.string().optional(),
  numberOfWeeks: z
    .number()
    .min(1, "Antall uker må være minst 1")
    .max(52, "Antall uker kan ikke overstige 52"),
  startDate: z.date({ required_error: "Startdato er påkrevd" }),
  recurringDayOfWeek: z.number().min(0).max(6),
  recurringTime: z
    .string()
    .regex(
      /^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$/,
      "Ugyldig tidsformat (bruk HH:MM)",
    ),
  duration: z
    .number()
    .min(15, "Varighet må være minst 15 minutter")
    .max(300, "Varighet kan ikke overstige 300 minutter"),
  capacity: z
    .number()
    .min(1, "Kapasitet må være minst 1")
    .max(100, "Kapasitet kan ikke overstige 100"),
  price: z.number().min(0, "Pris kan ikke være negativ"),
  location: z.string().min(2, "Lokasjon må være minst 2 tegn"),
  tags: z.string().optional(),
});

type CourseFormValues = z.infer<typeof courseFormSchema>;

const WEEKDAYS = [
  { value: 1, label: "Mandag" },
  { value: 2, label: "Tirsdag" },
  { value: 3, label: "Onsdag" },
  { value: 4, label: "Torsdag" },
  { value: 5, label: "Fredag" },
  { value: 6, label: "Lørdag" },
  { value: 0, label: "Søndag" },
];

export default function CourseCreate() {
  const { t } = useTranslation();
  const navigate = useNavigate();
  const { user } = useAuthContext();
  const { create, loading, error } = useCourseMutations();
  const [submitError, setSubmitError] = useState<string | null>(null);

  const form = useForm<CourseFormValues>({
    resolver: zodResolver(courseFormSchema),
    defaultValues: {
      name: "",
      description: "",
      numberOfWeeks: 8,
      startDate: new Date(),
      recurringDayOfWeek: 1, // Monday
      recurringTime: "18:00",
      duration: 75,
      capacity: 15,
      price: 1500,
      location: "",
      tags: "",
    },
  });

  const onSubmit = async (values: CourseFormValues) => {
    try {
      setSubmitError(null);

      if (!user?.id) {
        setSubmitError("Du må være innlogget for å opprette et kurs");
        return;
      }

      // Parse tags
      const tags = values.tags
        ? values.tags
            .split(",")
            .map((tag) => tag.trim())
            .filter(Boolean)
        : undefined;

      await create({
        teacherId: user.id,
        name: values.name,
        description: values.description || undefined,
        numberOfWeeks: values.numberOfWeeks,
        startDate: values.startDate,
        recurringDayOfWeek: values.recurringDayOfWeek,
        recurringTime: values.recurringTime,
        duration: values.duration,
        capacity: values.capacity,
        price: values.price,
        location: values.location,
        tags,
        sessions: [], // Sessions will be auto-generated by the service
      });

      // Navigate back to courses list on success
      navigate(ROUTES.TEACHER.COURSES);
    } catch (err) {
      setSubmitError(
        err instanceof Error ? err.message : "Kunne ikke opprette kurs",
      );
    }
  };

  return (
    <TeacherLayout>
      <div className="space-y-6">
        {/* Breadcrumb */}
        <Breadcrumb>
          <BreadcrumbList>
            <BreadcrumbItem>
              <BreadcrumbLink href={ROUTES.TEACHER.DASHBOARD}>
                Hjem
              </BreadcrumbLink>
            </BreadcrumbItem>
            <BreadcrumbSeparator />
            <BreadcrumbItem>
              <BreadcrumbLink href={ROUTES.TEACHER.COURSES}>
                Kursrekker
              </BreadcrumbLink>
            </BreadcrumbItem>
            <BreadcrumbSeparator />
            <BreadcrumbItem>
              <BreadcrumbPage>Opprett nytt</BreadcrumbPage>
            </BreadcrumbItem>
          </BreadcrumbList>
        </Breadcrumb>

        {/* Page Header */}
        <div>
          <h1 className="text-3xl font-semibold text-foreground">
            Opprett ny kursrekke
          </h1>
          <p className="mt-2 text-muted-foreground">
            Fyll ut skjemaet for å opprette et nytt yogakurs
          </p>
        </div>

        {/* Error Display */}
        {(submitError || error) && (
          <div className="rounded-lg border border-red-200 bg-red-50 p-4 text-red-800">
            <p className="font-medium">Feil ved opprettelse:</p>
            <p className="text-sm mt-1">{submitError || error?.message}</p>
          </div>
        )}

        {/* Form */}
        <div className="rounded-lg border border-border bg-white p-6">
          <Form {...form}>
            <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-6">
              {/* Name */}
              <FormField
                control={form.control}
                name="name"
                render={({ field }) => (
                  <FormItem>
                    <FormLabel>Kursnavn *</FormLabel>
                    <FormControl>
                      <Input
                        placeholder="F.eks. 8-ukers Hatha Yoga Grunnkurs"
                        {...field}
                      />
                    </FormControl>
                    <FormDescription>
                      Navnet på kurset som vises til studentene
                    </FormDescription>
                    <FormMessage />
                  </FormItem>
                )}
              />

              {/* Description */}
              <FormField
                control={form.control}
                name="description"
                render={({ field }) => (
                  <FormItem>
                    <FormLabel>Beskrivelse</FormLabel>
                    <FormControl>
                      <Textarea
                        placeholder="Beskriv kurset..."
                        className="min-h-[100px]"
                        {...field}
                      />
                    </FormControl>
                    <FormDescription>
                      En detaljert beskrivelse av hva kurset innebærer
                    </FormDescription>
                    <FormMessage />
                  </FormItem>
                )}
              />

              {/* Number of Weeks and Start Date Row */}
              <div className="grid gap-4 md:grid-cols-2">
                {/* Number of Weeks */}
                <FormField
                  control={form.control}
                  name="numberOfWeeks"
                  render={({ field }) => (
                    <FormItem>
                      <FormLabel>Antall uker *</FormLabel>
                      <FormControl>
                        <Input
                          type="number"
                          {...field}
                          onChange={(e) =>
                            field.onChange(parseInt(e.target.value))
                          }
                        />
                      </FormControl>
                      <FormDescription>
                        Hvor mange uker kurset varer
                      </FormDescription>
                      <FormMessage />
                    </FormItem>
                  )}
                />

                {/* Start Date */}
                <FormField
                  control={form.control}
                  name="startDate"
                  render={({ field }) => (
                    <FormItem className="flex flex-col">
                      <FormLabel>Startdato *</FormLabel>
                      <Popover>
                        <PopoverTrigger asChild>
                          <FormControl>
                            <Button
                              variant="outline"
                              className="w-full justify-start text-left font-normal"
                            >
                              <CalendarIcon className="mr-2 h-4 w-4" />
                              {field.value ? (
                                format(field.value, "dd.MM.yyyy")
                              ) : (
                                <span>Velg dato</span>
                              )}
                            </Button>
                          </FormControl>
                        </PopoverTrigger>
                        <PopoverContent className="w-auto p-0" align="start">
                          <Calendar
                            mode="single"
                            selected={field.value}
                            onSelect={field.onChange}
                            disabled={(date) =>
                              date < new Date(new Date().setHours(0, 0, 0, 0))
                            }
                            initialFocus
                          />
                        </PopoverContent>
                      </Popover>
                      <FormDescription>Når første økt er</FormDescription>
                      <FormMessage />
                    </FormItem>
                  )}
                />
              </div>

              {/* Weekday and Time Row */}
              <div className="grid gap-4 md:grid-cols-2">
                {/* Recurring Day */}
                <FormField
                  control={form.control}
                  name="recurringDayOfWeek"
                  render={({ field }) => (
                    <FormItem>
                      <FormLabel>Ukedag *</FormLabel>
                      <Select
                        onValueChange={(value) =>
                          field.onChange(parseInt(value))
                        }
                        value={field.value.toString()}
                      >
                        <FormControl>
                          <SelectTrigger>
                            <SelectValue placeholder="Velg ukedag" />
                          </SelectTrigger>
                        </FormControl>
                        <SelectContent>
                          {WEEKDAYS.map((day) => (
                            <SelectItem
                              key={day.value}
                              value={day.value.toString()}
                            >
                              {day.label}
                            </SelectItem>
                          ))}
                        </SelectContent>
                      </Select>
                      <FormDescription>
                        Hvilken dag i uken kurset går
                      </FormDescription>
                      <FormMessage />
                    </FormItem>
                  )}
                />

                {/* Recurring Time */}
                <FormField
                  control={form.control}
                  name="recurringTime"
                  render={({ field }) => (
                    <FormItem>
                      <FormLabel>Tidspunkt *</FormLabel>
                      <FormControl>
                        <Input type="time" {...field} />
                      </FormControl>
                      <FormDescription>
                        Klokkeslett for hver økt
                      </FormDescription>
                      <FormMessage />
                    </FormItem>
                  )}
                />
              </div>

              {/* Duration, Capacity, Price Row */}
              <div className="grid gap-4 md:grid-cols-3">
                {/* Duration */}
                <FormField
                  control={form.control}
                  name="duration"
                  render={({ field }) => (
                    <FormItem>
                      <FormLabel>Varighet (min) *</FormLabel>
                      <FormControl>
                        <Input
                          type="number"
                          {...field}
                          onChange={(e) =>
                            field.onChange(parseInt(e.target.value))
                          }
                        />
                      </FormControl>
                      <FormMessage />
                    </FormItem>
                  )}
                />

                {/* Capacity */}
                <FormField
                  control={form.control}
                  name="capacity"
                  render={({ field }) => (
                    <FormItem>
                      <FormLabel>Kapasitet *</FormLabel>
                      <FormControl>
                        <Input
                          type="number"
                          {...field}
                          onChange={(e) =>
                            field.onChange(parseInt(e.target.value))
                          }
                        />
                      </FormControl>
                      <FormMessage />
                    </FormItem>
                  )}
                />

                {/* Price */}
                <FormField
                  control={form.control}
                  name="price"
                  render={({ field }) => (
                    <FormItem>
                      <FormLabel>Pris (NOK) *</FormLabel>
                      <FormControl>
                        <Input
                          type="number"
                          {...field}
                          onChange={(e) =>
                            field.onChange(parseInt(e.target.value))
                          }
                        />
                      </FormControl>
                      <FormDescription>
                        Totalpris for hele kurset
                      </FormDescription>
                      <FormMessage />
                    </FormItem>
                  )}
                />
              </div>

              {/* Location */}
              <FormField
                control={form.control}
                name="location"
                render={({ field }) => (
                  <FormItem>
                    <FormLabel>Lokasjon *</FormLabel>
                    <FormControl>
                      <Input placeholder="F.eks. Yoga Studio Oslo" {...field} />
                    </FormControl>
                    <FormDescription>Hvor kurset finner sted</FormDescription>
                    <FormMessage />
                  </FormItem>
                )}
              />

              {/* Tags */}
              <FormField
                control={form.control}
                name="tags"
                render={({ field }) => (
                  <FormItem>
                    <FormLabel>Tags</FormLabel>
                    <FormControl>
                      <Input
                        placeholder="F.eks. Hatha, Nybegynner, Kveldskurs"
                        {...field}
                      />
                    </FormControl>
                    <FormDescription>
                      Kommaseparerte tags (valgfritt)
                    </FormDescription>
                    <FormMessage />
                  </FormItem>
                )}
              />

              {/* Info */}
              <div className="rounded-lg border border-blue-200 bg-blue-50 p-4">
                <p className="text-sm text-blue-800">
                  <strong>Info:</strong> Kursøkter blir automatisk generert
                  basert på startdato, antall uker og valgt ukedag.
                </p>
              </div>

              {/* Form Actions */}
              <div className="flex gap-4">
                <Button type="submit" disabled={loading}>
                  {loading ? "Oppretter..." : "Opprett kurs"}
                </Button>
                <Button
                  type="button"
                  variant="outline"
                  onClick={() => navigate(ROUTES.TEACHER.COURSES)}
                  disabled={loading}
                >
                  Avbryt
                </Button>
              </div>
            </form>
          </Form>
        </div>
      </div>
    </TeacherLayout>
  );
}
